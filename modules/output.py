def format_output(row):
  row_data = row[0]
  inference_result = row[1].inference

  # format your output below, then return it
  # output should be a dictionary, and it should match the schema in schema.py

  return output